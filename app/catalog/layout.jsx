export const metadata = {
  title: {
    default: "Catalog",
    template: "%s | Catalog",
  },
  description: "Browse canonical dental products and price comparisons.",
}

export default function CatalogLayout({ children }) {
  return children
}
